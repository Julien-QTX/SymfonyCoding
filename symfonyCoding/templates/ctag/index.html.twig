{% extends 'base.html.twig' %}

{% block title %}Liste des Tags{% endblock %}

{% block body %}
    <h1>Liste des Tags</h1>

    <ul>
        {% for tag in tags %}
            <li>{{ tag.name }}</li>
        {% endfor %}
    </ul>

    <a href="{{ path('app_ctag_create') }}">Créer un Tag</a>
{% endblock %}
